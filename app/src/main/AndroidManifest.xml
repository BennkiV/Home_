<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.quick_home">
    <!-- _________________ For location required ____________________________ -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" /> <!-- _________________ For internet connection __________________________ -->
    <uses-permission android:name="android.permission.INTERNET" /> <!-- _____________________________________________________________________ -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/home_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/home_icon_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Quick_Home">
        <activity android:name=".TransitConnection"></activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>